data:
  data_dir: "data/"
  data_name: "gait.npz"
  data_loader: "new_loader"

feature:
  feature_extractor: "minirocket"

split:
  split_type: "train_val_test_split"

train:
  model_name: "gait_minirocket_svm_linear"
  model_type: "svm"
  model_dir: "model"
  report_dir: "reports"